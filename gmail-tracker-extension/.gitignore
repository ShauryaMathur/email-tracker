node_modules/

# Compiled output — generated by `npm run build` from src/*.ts. Source of
# truth is src/, these are build artifacts (kept out of git same as any
# other compiled language, but note the *unpacked* extension folder needs
# them physically present on disk to load in Chrome).
/content.js
/auth-bridge.js
