{
  "name": "email-tracker-extension",
  "version": "2.0.0",
  "private": true,
  "description": "Multi-provider (Gmail/Outlook/Yahoo) email open-tracking Chrome extension",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.279",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
